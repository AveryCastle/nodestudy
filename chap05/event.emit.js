process.on('exit', function(){
	console.log('good bye~ take care of yourself ~~ ');
});


process.exit(0);

process.emit('exit');
process.emit('exit');

//process.exit(0);

process.emit('exit');
process.emit('exit');
process.emit('exit');

console.log('the program is running now');